# NewsAPI Key (Required for fetching news)
# Get one at https://newsapi.org/
NEWS_API_KEY=your_newsapi_key_here

# LLM Provider Configuration
# Default settings loaded on server startup.
# You can change these temporarily in the UI, but .env sets the permanent defaults.
# Options: "Google Gemini" or "Ollama"
LLM_PROVIDER=Google Gemini

# Google Gemini API Key (Required if LLM_PROVIDER is "Google Gemini")
# Get one at https://aistudio.google.com/
GEMINI_API_KEY=your_gemini_key_here

# Ollama Configuration (Required if LLM_PROVIDER is "Ollama")
# Ensure Ollama is running locally (usually at http://localhost:11434)
OLLAMA_MODEL=llama3.1:8b
OLLAMA_BASE_URL=http://localhost:11434

# Application Settings
# Default country for "Trending" news (e.g., us, in, gb)
DEFAULT_COUNTRY=us

# Cache duration in seconds (default: 300)
CACHE_TTL_SEC=300

# Maximum token limit for context window (default: 4000)
MAX_TOKENS=4000